#include "Vertex.h"


// constructor
Vertex::Vertex(vec3 inPos, vec3 inNormal, vec3 inColor, vec2 inTexCoord):
    pos(inPos),
    normal(inNormal),
    color(inColor),
    texCoord(inTexCoord)
    { }

